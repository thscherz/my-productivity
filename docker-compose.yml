services:
  my-productivity:
    build: .
    container_name: my-productivity
    ports:
      - "8002:8002"
    volumes:
      # SQLite-Datenbank persistent auf dem Host speichern
      - ./data:/app/backend/data
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8002/api/v1/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
